# شمای دفتر حسابداری — ۱۷ شیت

اسپردشیت: `12j12zy8lqxlft1mBIZ0THmG-Ro24TZmUqijVwMpuTg4`

نماد ✍️ = شما پر می‌کنید · 🤖 = خودکار پر می‌شود · 🔒 = دست نزنید

---

## ۱. راهنما 🤖
| ستون | توضیح |
|---|---|
| بخش | نام شیت |
| توضیح | کاربرد آن شیت |

## ۲. تنظیمات ✍️
| ستون | توضیح |
|---|---|
| کلید | نام تنظیم (تغییرش ندهید) |
| مقدار | مقداری که شما تعیین می‌کنید |
| توضیح | راهنمای همان کلید |

کلیدهای مهم:

| کلید | مقدار پیش‌فرض | اثر |
|---|---|---|
| `نام_آموزشگاه` | آموزشگاه علمی فردا | سربرگ ایمیل‌ها |
| `واحد_پول` | تومان | فقط برچسب |
| `درصد_پیشفرض_حضوری` | 70 | وقتی برای مدرس/کلاس درصدی تعریف نشده |
| `درصد_پیشفرض_آنلاین` | 60 | همان، برای کلاس آنلاین |
| `ایمیل_گزارش` | f.shams.apg@gmail.com | گیرنده گزارش‌ها (چند آدرس با ویرگول) |
| `ارسال_فیش_به_مدرس` | خیر | رزرو برای توسعه بعدی |
| `مبنای_تعدیل_سرمایه` | طلا | `طلا` \| `دلار` \| `تورم_رسمی` |
| `درصد_مالیات` | 0 | کسر از حقوق مدرس |
| `درصد_بیمه` | 0 | کسر از حقوق مدرس |
| `سهم_سرمایه_گذار_از_سود` | 0 | رزرو برای وقتی تقسیم سود شروع شود |

## ۳. مدرسین ✍️
`کد_مدرس` · `نام_مدرس` · `ایمیل` · `موبایل` · `شماره_شبا` · `درصد_حضوری` · `درصد_آنلاین` · `نوع_قرارداد` · `حقوق_ثابت_ماهانه` · `وضعیت` · `توضیح`

- `نوع_قرارداد`: `درصدی` | `ثابت` | `ترکیبی` — فقط در دو حالت آخر `حقوق_ثابت_ماهانه` به حقوق اضافه می‌شود.
- `وضعیت` = `غیرفعال` مدرس را از فرم و از محاسبه حقوق حذف می‌کند بدون اینکه سابقه‌اش پاک شود.

## ۴. کلاس‌ها ✍️
`کد_کلاس` · `نام_کلاس` · `کد_مدرس` · `نوع_کلاس` · `شهریه_هر_نفر` · `تعداد_جلسات_دوره` · `درصد_مدرس_اختصاصی` · `ظرفیت` · `وضعیت` · `توضیح`

- `نوع_کلاس`: باید دقیقاً `حضوری` یا `آنلاین` باشد (تشخیص آنلاین با وجود کلمه «آنلاین» انجام می‌شود).
- `درصد_مدرس_اختصاصی` خالی = از درصد خود مدرس استفاده شود.

## ۵. سرمایه‌گذاران ✍️
`کد_سرمایه_گذار` · `نام_سرمایه_گذار` · `ایمیل` · `موبایل` · `تاریخ_شروع_همکاری` · `درصد_توافقی_سود` · `وضعیت` · `توضیح`

## ۶. جلسات_تدریس ⚠️ بازنشسته
از این شیت دیگر استفاده نمی‌شود. جای آن را «برنامه_کلاس‌ها» (برای سابقهٔ برگزاری) و ستون `سهم_مدرس` شیت «درآمد» (برای حقوق) گرفته‌اند. ردیف‌های قدیمی‌اش دست‌نخورده می‌مانند ولی در هیچ محاسبه‌ای نمی‌آیند.

<details><summary>ساختار قدیمی</summary>

`شناسه` · `تاریخ_شمسی` · `تاریخ_میلادی` · `سال_شمسی` · `ماه_شمسی` · `کد_مدرس` · `نام_مدرس` · `کد_کلاس` · `نام_کلاس` · `نوع_کلاس` · `تعداد_جلسه` · `تعداد_فراگیر` · `درآمد_ناخالص` · `درصد_مدرس` · `سهم_مدرس` · `وضعیت_تسویه` · `توضیح` · `منبع` · `زمان_ثبت`

</details>

## ۷. درآمد 🤖
`شناسه` · `تاریخ_شمسی` · `تاریخ_میلادی` · `سال_شمسی` · `ماه_شمسی` · `نوع_درآمد` · `پرداخت_کننده` · `کد_کلاس` · `نام_کلاس` · `کد_مدرس` · `مبلغ` · `روش_پرداخت` · `شماره_پیگیری` · `توضیح` · `منبع` · `زمان_ثبت` · `ثبت_کننده` · `لینک_فاکتور` · `نوع_پرداخت_کننده` · `نوع_پرداخت` · `نام_مدرس` · `درصد_مدرس` · `سهم_مدرس` · `سهم_آموزشگاه`

`نوع_درآمد` خودکار از روی «پرداخت‌کننده چه کسی است» ساخته می‌شود: `شهریه فراگیر` | `مجموعه همکار` | `سفارش پروژه` | `سایر`. در گزارش، «شهریه» و «همکار» جدا شمرده می‌شوند؛ بقیه در «سایر».

- `ثبت_کننده` = نامی که در فرم وارد شده؛ فقط برای ردگیری، در هیچ محاسبه‌ای دخالت ندارد.
- `لینک_فاکتور` = لینک گوگل‌درایو فایلی که در فرم آپلود شده (چند فایل با ` | ` جدا می‌شوند).
- `منبع` اگر «(بدون تأیید)» داشته باشد یعنی موقع ثبت، شناسه چت تلگرام تنظیم نبوده و ردیف مستقیم نوشته شده.
- `نوع_پرداخت_کننده`: `فراگیر` | `مدرسه یا سازمان` | `مجموعه همکار` | `سفارش‌دهنده پروژه` | `سایر` — همین ستون `نوع_درآمد` را تعیین می‌کند.
- `نوع_پرداخت`: `پرداخت یکجا` | `پیش‌پرداخت` | `پس‌پرداخت` | `پرداخت ماهانه` | `پرداخت قسطی` | `سایر`.
- `نام_کلاس` اگر دوره در شیت «کلاس‌ها» نبود، عیناً همان چیزی که در فرم نوشته‌اید ثبت می‌شود تا اطلاعات گم نشود.

### سه ستون سهم — قلب کار درصدی

| ستون | چطور پر می‌شود |
|---|---|
| `درصد_مدرس` | اولین مقداری که پر باشد: ۱) عددی که در فرم نوشتید ۲) `درصد_مدرس_اختصاصی` همان دوره ۳) `درصد_حضوری`/`درصد_آنلاین` خود مدرس ۴) درصد پیش‌فرض در «تنظیمات» |
| `سهم_مدرس` | `مبلغ × درصد_مدرس ÷ ۱۰۰` (گرد شده) — اگر مدرسی مشخص نشود، صفر |
| `سهم_آموزشگاه` | `مبلغ − سهم_مدرس` |

⚠️ این سه ستون **در لحظه ثبت قفل می‌شوند**. اگر بعداً درصد یک مدرس را عوض کنید، واریزی‌های گذشته دست‌نخورده می‌مانند — که همان رفتار درست حسابداری است.

## ۸. هزینه‌ها 🤖
`شناسه` · `تاریخ_شمسی` · `تاریخ_میلادی` · `سال_شمسی` · `ماه_شمسی` · `دسته_هزینه` · `شرح` · `مبلغ` · `روش_پرداخت` · `طرف_حساب` · `نوع_هزینه` · `توضیح` · `منبع` · `زمان_ثبت` · `ثبت_کننده` · `لینک_فاکتور`

- `دسته_هزینه`: اجاره، قبوض، اینترنت و تلفن، **شارژ ساختمان**، تبلیغات، لوازم مصرفی، تعمیر و نگهداری، حقوق پرسنل اداری، **پاداش**، **هوش مصنوعی**، **پشتیبانی ایجنت و موارد مرتبط**، ایاب و ذهاب، پذیرایی، مالیات، کارمزد بانکی، سایر. اگر «سایر» را انتخاب کردید، در «شرح» دقیق بنویسید چه بوده.
- `ثبت_کننده` و `لینک_فاکتور` مثل شیت درآمد کار می‌کنند — نام کسی که هزینه را ثبت کرده و لینک عکس/PDF فاکتورش.

⚠️ حقوق مدرسین را اینجا ثبت **نکنید** — خودکار محاسبه و در گزارش جدا آورده می‌شود، وگرنه دوبار حساب می‌شود.

## ۹. آورده_سرمایه 🤖✍️
`شناسه` · `تاریخ_شمسی` · `تاریخ_میلادی` · `سال_شمسی` · `ماه_شمسی` · `کد_سرمایه_گذار` · `نام_سرمایه_گذار` · `نوع_آورده` · `شرح_آورده` · `مبلغ_تومان` · `نرخ_طلای18_روز` · `نرخ_دلار_روز` · `معادل_گرم_طلا` · `معادل_دلار` · `دسته` · `توضیح` · `منبع` · `زمان_ثبت` · `شماره_فیش` · `ثبت_کننده`

از فرم مستقل آورده سرمایه (ورک‌فلوی ۰۷) پر می‌شود. آدرس آن فرم عمومی نیست و در پنل خصوصی مدیر است.

- `نوع_آورده`: `نقدی` | `کالا`
- `دسته`: `سرمایه اولیه (سال‌های قبل)` | `آورده ماهانه` | `آورده متفرقه`
- برای آورده‌های قدیمی که دستی وارد می‌کنید، ستون‌های نرخ و معادل را خالی بگذارید — سیستم از تورم رسمی استفاده می‌کند. حتماً `سال_شمسی` را درست بزنید.

## ۱۰. حقوق_مدرسین 🤖🔒
`شناسه_دوره` · `سال_شمسی` · `ماه_شمسی` · `کد_مدرس` · `نام_مدرس` · `ایمیل` · `جلسات_حضوری` · `جلسات_آنلاین` · `درآمد_منتسب_حضوری` · `درآمد_منتسب_آنلاین` · `درصد_حضوری` · `درصد_آنلاین` · `حقوق_درصدی` · `حقوق_ثابت` · `کسورات` · `خالص_پرداختی` · `شماره_شبا` · `وضعیت_پرداخت` · `تاریخ_محاسبه`

`شناسه_دوره` = `1405-05|M001` (کلید یکتا برای upsert). ستون `وضعیت_پرداخت` را می‌توانید بعد از واریز دستی به «پرداخت شد» تغییر دهید.

## ۱۱. گزارش_ماهانه 🤖🔒
`شناسه_دوره` · `سال_شمسی` · `ماه_شمسی` · `نام_ماه` · `درآمد_شهریه` · `درآمد_همکاری` · `درآمد_سایر` · `جمع_درآمد` · `حقوق_مدرسین` · `سایر_هزینه‌ها` · `جمع_هزینه` · `سود_عملیاتی` · `حاشیه_سود_درصد` · `آورده_سرمایه_گذار` · `جریان_نقد_خالص` · `تعداد_جلسات` · `تعداد_فراگیر` · `تاریخ_تولید`

`شناسه_دوره` = `1405-05`

## ۱۲. گزارش_دوره‌ای 🤖🔒
`نوع_دوره` · `شناسه_دوره` · `سال_شمسی` · `از_ماه` · `تا_ماه` · `جمع_درآمد` · `جمع_هزینه` · `حقوق_مدرسین` · `سود` · `حاشیه_سود_درصد` · `میانگین_سود_ماهانه` · `رشد_نسبت_به_دوره_قبل_درصد` · `آورده_سرمایه_دوره` · `تاریخ_تولید`

`شناسه_دوره` = `1405-Q1` / `1405-H1` / `1405-Y`

## ۱۳. ارزش_سرمایه 🤖🔒
`تاریخ_محاسبه` · `سال_شمسی` · `کد_سرمایه_گذار` · `نام_سرمایه_گذار` · `جمع_آورده_اسمی` · `معادل_گرم_طلا` · `معادل_دلار` · `نرخ_طلای18_امروز` · `نرخ_دلار_امروز` · `ارزش_روز_طلایی` · `ارزش_روز_دلاری` · `ارزش_روز_تورمی` · `سود_تورمی_تحقق_نیافته` · `درصد_از_کل_سرمایه`

یک ردیف به‌ازای هر سرمایه‌گذار که در هر اجرا به‌روز می‌شود (نه ردیف جدید).

## ۱۴. شاخص_سالانه ✍️
`سال_شمسی` · `نرخ_تورم_رسمی_درصد` · `نرخ_دلار_پایان_سال` · `نرخ_طلای18_پایان_سال` · `منبع` · `توضیح`

سالی یک بار، بعد از انتشار آمار رسمی، یک ردیف را کامل کنید.
⚠️ اعداد فعلی ۱۳۹۵ تا ۱۴۰۳ **تقریبی** هستند و باید با منبع رسمی بازبینی شوند.

## ۱۵. لاگ_ورودی 🤖🔒
`زمان` · `منبع` · `نوع_رکورد` · `شناسه_مرتبط` · `وضعیت` · `پیام` · `داده_خام`

بایگانی خام همه ثبت‌ها برای ردیابی خطا.

## ۱۶. برنامه_کلاس‌ها 🤖
`شناسه` · `تاریخ_شمسی` · `تاریخ_میلادی` · `سال_شمسی` · `ماه_شمسی` · `روز_هفته` · `کد_کلاس` · `نام_کلاس` · `نوع_کلاس` · `کد_مدرس` · `نام_مدرس` · `ساعت` · `تعداد_جلسه` · `تعداد_فراگیر` · `وضعیت` · `توضیح` · `منبع` · `زمان_ثبت`

سابقهٔ کلاس‌هایی که طی برنامهٔ هفتگی برگزار شده‌اند. از فرم `‎/form/sabt-farda` پر می‌شود.

- `وضعیت`: `برگزار شد` | `لغو شد` | `جبرانی` — ردیف «لغو شد» در شمارش جلسات گزارش نمی‌آید.
- ⚠️ **این شیت پول حساب نمی‌کند.** حقوق مدرس از ستون `سهم_مدرس` روی واریزی‌های شیت «درآمد» می‌آید. این شیت فقط تعداد جلسات و حضور فراگیر را برای گزارش ماهانه می‌دهد.

## ۱۷. برنامه_هفتگی 🤖
`شناسه` · `نام_فراگیر` · `موبایل_فراگیر` · `کد_کلاس` · `نام_کلاس` · `نوع_کلاس` · `قالب_کلاس` · `مکان` · `کد_مدرس` · `نام_مدرس` · `روز_هفته` · `ساعت_شروع` · `ساعت_پایان` · `تاریخ_شروع` · `وضعیت` · `توضیح` · `منبع` · `زمان_ثبت`

برنامهٔ ثابت هفتهٔ آموزشگاه: چه فراگیری، چه کلاسی، چه روزی، چه ساعتی. از فرم `‎/form/sabt-farda` پر می‌شود.

- `نوع_کلاس`: حضوری | آنلاین | ترکیبی — `قالب_کلاس`: خصوصی | نیمه‌خصوصی | گروهی | کارگاه — `مکان`: کلاس ۱ | کلاس ۲ | آنلاین | سایر.
- **هر ردیف = یک جلسهٔ ثابت در هفته.** موقع ثبت، «تعداد روز در هفته» را می‌پرسد و برای هر روز، روز و ساعتش را جدا می‌گیرد؛ هر روز یک ردیف می‌شود. فراگیری که دو روز در هفته می‌آید دو ردیف دارد.
- `روز_هفته`: شنبه تا جمعه — `وضعیت`: `فعال` | `متوقف` | `تمام شده`
- ⚠️ **این شیت هم پول حساب نمی‌کند.** فقط برنامه‌ریزی و پاسخ به «فردا چه کلاسی داریم؟».

---

## قواعدی که نباید بشکنند

1. **سرستون‌ها را تغییر نام ندهید.** نگاشت ورک‌فلوها بر اساس همین نام‌هاست.
2. **ستون جدید فقط در انتهای هر شیت** اضافه کنید.
3. **`سال_شمسی` و `ماه_شمسی` عدد باشند**، نه متن — فیلتر دوره‌ها روی همین دو ستون است.
4. **حقوق مدرسین را در شیت هزینه‌ها ننویسید.**
5. اگر ردیفی را دستی اصلاح کردید، ورک‌فلوی ۰۳ را دستی اجرا کنید تا آن ماه بازمحاسبه شود.
