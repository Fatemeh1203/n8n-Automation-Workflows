# سیستم حسابداری آموزشگاه علمی فردا — اتوماسیون n8n

اتوماسیون کامل حسابداری آموزشگاه روی n8n + گوگل‌شیت، با تقویم شمسی، محاسبه‌ی خودکار حقوق درصدی مدرسین، گزارش‌های دوره‌ای و ارزش‌گذاری تورمی سرمایه‌گذاران.

> ### 🌐 [صفحهٔ همکاران (گیت‌هاب‌پیج) ←](https://fatemeh1203.github.io/n8n-Automation-Workflows/)
>
> این را به همکاران بدهید: فقط ثبت واریزی، ثبت هزینه و کلاس‌ها. سورسش `docs/index.html` است.
>
> ### 🔒 پنل مدیر — خصوصی
>
> آورده سرمایه، دفتر گوگل‌شیت، پنل n8n، تنظیم تلگرام و وبهوک آنجا هستند و پشت رمزِ خودِ مدیرند. **آدرسش عمداً در این مخزن عمومی نیامده.**

---

> ### 📒 [راهنمای تصویری کامل را اینجا باز کنید ←](https://claude.ai/code/artifact/c3162cc8-061c-4423-8cf3-f8f374227cc7)
>
> یک صفحه‌ی فارسی و راست‌چین با نمودارهای واقعی: نقشه‌ی جریان داده، اینکه هر ورک‌فلو دقیقاً چه می‌خواند و چه می‌نویسد، آبشار تعیین درصد مدرس، تفاوت درآمد با آورده‌ی سرمایه‌گذار، روش ارزش‌گذاری تورمی، نقشه‌ی شیت‌ها، چک‌لیست راه‌اندازی و جدول عیب‌یابی.
>
> سورس همان صفحه: [`docs/guide.artifact.html`](docs/guide.artifact.html)

---

## دفتر حسابداری (گوگل‌شیت)

**لینک:** https://docs.google.com/spreadsheets/d/12j12zy8lqxlft1mBIZ0THmG-Ro24TZmUqijVwMpuTg4/edit
**شناسه:** `12j12zy8lqxlft1mBIZ0THmG-Ro24TZmUqijVwMpuTg4`

۱۷ شیت راست‌چین، سرستون فارسی، سطر اول قفل، مبالغ با جداکننده هزارگان.

---

## ورک‌فلوها

| # | نام | شناسه | نوع اجرا |
|---|-----|-------|----------|
| ۰۰ | راه‌اندازی دفتر حسابداری | `jPCxKxQxTr7lLtax` | دستی — یک بار (انجام شد) |
| ۰۱ | کلاس‌های برگزارشده و برنامهٔ هفتگی | `40Qh2f0Bm3B2VcqF` | فرم وب `sabt-farda` |
| ۰۲ | واردات دسته‌ای از فایل اکسل | `Hl2ABCF6U7Z0lZZI` | فرم آپلود + پیوست Gmail (هر ۱۵ دقیقه) |
| ۰۳ | بستن ماه، حقوق مدرسین و گزارش ماهانه | `ibtnu3CNB8RcJl8D` | روزانه ۲۳:۳۰ + دستی |
| ۰۴ | گزارش ۳/۶/۱۲ ماهه و ارزش تورمی سرمایه | `nQ9LaOcXd6hylOWz` | روزانه ۲۳:۴۵ + دستی |
| ۰۵ | ثبت واریزی و هزینه با تأیید تلگرامی | `EP0X5nQgGxUGSqMj` | دو فرم جدا (واریزی/هزینه) + وبهوک POST، با تأیید در تلگرام |
| ۰۶ | تنظیم شناسه چت تلگرام | `qeqpI8Ue487XBMBW` | فرم یک‌بارمصرف برای روشن‌کردن تأیید تلگرامی |
| ۰۷ | ثبت آورده سرمایه‌گذار | `h0g5qfadXBC5VzSv` | فرم وب — آدرس در پنل خصوصی |

منطقه زمانی همه‌ی ورک‌فلوها روی `Asia/Tehran` تنظیم شده. ورک‌فلوهای **۰۱ تا ۰۷ فعال (Published)** هستند؛ ورک‌فلوی ۰۰ عمداً غیرفعال است چون کارش یک‌بار انجام شده و اجرای دوباره‌اش فایده‌ای ندارد.

⚠️ در پنل n8n ورک‌فلوهای دیگری هم می‌بینید (طلافروشی، ربات مدرسه، دستیار فریلنسر و…) که به این پروژه ربطی ندارند. یک نسخه‌ی تکراری و بی‌تریگر از «۰۱» هم با شناسه‌ی `BqUzKOHIXRVWxe7D` باقی مانده که هیچ‌وقت اجرا نمی‌شود — نسخه‌ی درست `40Qh2f0Bm3B2VcqF` است.

همه‌ی نودهای تلگرام (چهار نود در ورک‌فلوی ۰۵ و یکی در ۰۶) روی اعتبارنامه‌ی **«ربات حسابداری»** تنظیم شده‌اند. ورک‌فلوهای ۰۱ تا ۰۴ و ۰۷ اصلاً تلگرام ندارند — گزارش‌هایشان با Gmail می‌رود.

### لینک فرم‌ها

**صفحهٔ همکاران:** https://fatemeh1203.github.io/n8n-Automation-Workflows/

| کاربرد | آدرس |
|---|---|
| 💰 ثبت واریزی | `https://arezoo000.app.n8n.cloud/form/daramad-farda` |
| 🧾 ثبت هزینه | `https://arezoo000.app.n8n.cloud/form/hazine-farda` |
| 🗓 کلاس برگزارشده و برنامهٔ هفتگی فراگیر | `https://arezoo000.app.n8n.cloud/form/sabt-farda` |
| 🚪 ورودی مشترک درآمد/هزینه | در پنل خصوصی مدیر |
| 🏦 آورده سرمایه‌گذار | در پنل خصوصی مدیر |
| ⚙️ تنظیم تلگرام (یک بار) | در پنل خصوصی مدیر |
| 📊 آپلود فایل اکسل | در پنل خصوصی مدیر |
| 🔌 وبهوک ورودی (POST) | در پنل خصوصی مدیر |

⚠️ **این مخزن عمومی است.** فقط آدرس فرم‌هایی که به همکاران می‌دهید اینجا آمده. آدرس بقیه عمداً نیامده؛ اگر جایی نوشتید، عملاً منتشرشان کرده‌اید.

فرم `sabt-farda` **فقط** برای **کلاس‌های برگزارشده** و **برنامهٔ کلاسی فراگیران** است و مستقیم در شیت می‌نویسد (بدون تأیید تلگرامی). آورده سرمایه‌گذار ورک‌فلوی مستقل خودش را دارد (۰۷). درآمد و هزینه از فرم کلاس‌ها برداشته شده‌اند تا دو مسیر موازی با اعداد ناسازگار به وجود نیاید.

> ⚠️ لیست کشویی «کلاس» و «سرمایه‌گذار» از شیت خوانده می‌شود و ردیف‌هایی که ستون `وضعیت` آن‌ها «نمونه» است نادیده گرفته می‌شوند. تا وقتی سرمایه‌گذار و کلاس واقعی وارد نکنید، فهرست خالی می‌ماند.

### وضعیت تست

| مسیر | نتیجه |
|---|---|
| ساخت اسپردشیت با ۱۵ شیت و قالب‌بندی | ✅ اجرا شد |
| WF-03 کامل تا ارسال ایمیل + پیوست اکسل | ✅ ایمیل ارسال شد |
| WF-04 کامل تا ارسال ایمیل | ✅ ایمیل ارسال شد |
| اتصال Nerkh API (طلا و دلار) | ✅ نرخ زنده دریافت شد |
| تبدیل تاریخ شمسی | ✅ ۴۰٬۰۰۰ روز، صفر خطا |
| نوشتن در گوگل‌شیت (append و upsert) | ✅ |
| WF-05 وبهوک: دریافت، نگاشت کلیدها، تشخیص نوع، ثبت لاگ | ✅ با سه نمونه واقعی تست شد |
| WF-05 بدون شناسه چت: ثبت مستقیم در شیت با ستون‌های تازه | ✅ ردیف نوشته شد، پیام تلگرام به‌درستی رد شد |
| آپلود فایل به گوگل‌درایو (آپلود + نام‌گذاری + انتقال به پوشه) | ✅ با یک فایل واقعی تست شد |
| فرم‌های WF-01 و WF-02 | ورودی انسانی لازم دارند — با باز کردن لینک تست کنید |
| WF-05 فرم با پیوست واقعی، سرتاسری | تست نشده — از این محیط به آدرس n8n دسترسی شبکه نبود؛ لطفاً یک بار خودتان با یک عکس امتحان کنید |
| WF-05 از راه فرم (بدون پیوست) | ✅ ردیف در شیت «درآمد» نوشته شد، تلگرام درست رد شد |
| WF-06 فرم تنظیم تلگرام | ✅ شناسه چت در «تنظیمات» ثبت شد |
| WF-05 مسیر کامل تا پیام تأیید تلگرام | ✅ پیام با دکمه‌ها ارسال و اجرا منتظر تصمیم شد |
| WF-07 فرم آورده سرمایه‌گذار | ✅ ساخته و منتشر شد — صفحهٔ فرم را یک بار باز کنید |
| WF-01 مسیر برنامهٔ هفتگی فراگیر | ورودی انسانی لازم دارد — با باز کردن `sabt-farda` تست کنید |
| WF-05 محاسبه سهم مدرس روی واریزی (زنده) | ✅ دوره C001 → مدرس M001 → ۷۰٪ → ۵۶۰٬۰۰۰ / ۲۴۰٬۰۰۰ |
| منطق نرمال‌سازی (۹ سناریو، آفلاین) | ✅ درصد اختصاصی، آنلاین/حضوری، دوره ناشناس، بدون مدرس، هزینه |
| منطق حقوق از روی واریزی (۹ سناریو، آفلاین) | ✅ حقوق درصدی، نرخ حضوری/آنلاین، قرارداد ترکیبی، مدرس خارج از شیت، کلاس لغوشده |
| WF-03 اجرای کامل با داده واقعی | ✅ سرتاسری اجرا شد و ایمیل گزارش شهریور ۱۴۰۵ ارسال شد. اعداد صفرند چون شیت «درآمد» هنوز هیچ ردیف داده‌ای ندارد — فقط سرستون |

---

## گردش کار روزمره

```
  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
  │ فرم واریزی   │   │ فایل اکسل    │   │  فرم هزینه   │
  │ +سهم مدرس    │   │ (فرم/ایمیل)  │   │  یا وبهوک    │
  └──────┬───────┘   └──────┬───────┘   └──────┬───────┘
         │  WF-05           │  WF-02           │  WF-05
         └──────────────────┼──────────────────┘
                            ▼
        ┌──────────────────────────────────────┐
        │  گوگل‌شیت: درآمد | هزینه‌ها           │
        │  برنامه_کلاس‌ها | برنامه_هفتگی        │
        │  آورده_سرمایه ← WF-07                │
        └──────────────────┬───────────────────┘
                           │ WF-03 (آخرین روز ماه شمسی)
                           ▼
        ┌──────────────────────────────────────┐
        │  حقوق_مدرسین + گزارش_ماهانه          │
        │  ← ایمیل گزارش ماه + پیوست اکسل      │
        └──────────────────┬───────────────────┘
                           │ WF-04 (پایان هر فصل شمسی)
                           ▼
        ┌──────────────────────────────────────┐
        │  گزارش_دوره‌ای (Q/H/سالانه)           │
        │  ارزش_سرمایه (تعدیل تورمی)            │
        │  ← ایمیل گزارش دوره‌ای                │
        └──────────────────────────────────────┘
```

---

## قدم‌های راه‌اندازی (یک‌بار)

1. **شیت «تنظیمات» را پر کنید** — مهم‌ترین‌ها:
   - `درصد_پیشفرض_حضوری` و `درصد_پیشفرض_آنلاین`
   - `ایمیل_گزارش` (چند آدرس را با ویرگول جدا کنید)
   - `مبنای_تعدیل_سرمایه`: `طلا` | `دلار` | `تورم_رسمی`
   - `درصد_مالیات` و `درصد_بیمه` (اگر از حقوق مدرس کسر می‌کنید)

2. **مدرسین را وارد کنید** — درصد حضوری و آنلاین هرکس جداگانه.

3. **کلاس‌ها را وارد کنید** — اگر درصد یک کلاس خاص با درصد پیش‌فرض مدرس فرق دارد، ستون `درصد_مدرس_اختصاصی` را پر کنید.

4. **سرمایه‌گذاران و آورده‌های قدیمی** — آورده‌های سال‌های گذشته را در شیت `آورده_سرمایه` وارد کنید (یا با اکسل بریزید). حتماً `سال_شمسی` را درست بزنید؛ مبنای محاسبه تورم همان است.

5. **ردیف‌های «(نمونه)» را حذف کنید.**

6. **لینک فرم‌ها را روی گوشی بوکمارک کنید.** ورک‌فلوها از قبل فعال‌اند؛ لازم نیست کاری در n8n بکنید.

7. **تأیید تلگرامی را روشن کنید (یک بار، حدود یک دقیقه):**
   - **اول به ربات حسابداری خودتان در تلگرام `/start` بزنید** — بدون این کار تلگرام اجازه نمی‌دهد ربات به شما پیام بدهد و خطای `chat not found` می‌گیرید.
   - در تلگرام به ربات `@get_id_bot` پیام بدهید و عدد `Your chat ID` را بردارید.
   - فرم «تنظیم تلگرام» را باز کنید و عدد را داخلش بگذارید (آدرسش در پنل خصوصی مدیر است)
   - بلافاصله یک **پیام تست** در تلگرام می‌گیرید. اگر آمد، کار تمام است.

   تا وقتی این کار را نکرده‌اید سیستم کار می‌کند، فقط ورودی‌ها **مستقیم و بدون تأیید** در شیت ثبت می‌شوند و در ستون `منبع` علامت «(بدون تأیید)» می‌خورند — چیزی از دست نمی‌رود.

---

## ثبت واریزی و هزینه (WF-05)

**سه در ورودی، یک مسیر تأیید.** هر سه به یک نقطه می‌رسند: خلاصه به تلگرام شما، و تا دکمه‌ی «✅ ثبت کن» را نزنید هیچ ردیفی نوشته نمی‌شود.

### ۱) فرم واریزی

```
https://arezoo000.app.n8n.cloud/form/daramad-farda
```

| فیلد | اجباری | نکته |
|---|:---:|---|
| پرداخت‌کننده چه کسی است؟ | ✳️ | فراگیر / مدرسه یا سازمان / مجموعه همکار / سایر — همین `نوع_درآمد` را تعیین می‌کند |
| نام پرداخت‌کننده | ✳️ | |
| مبلغ (تومان) | ✳️ | |
| تاریخ پرداخت | ✳️ | |
| دوره / درس | ✳️ | کد یا نام دوره. اگر دوره‌ای ندارد بنویسید «ندارد» |
| نام مدرس این دوره | | خالی = از روی دوره در شیت «کلاس‌ها» پیدا می‌شود |
| درصد مدرس از این پرداخت | | خالی = از درصد ثبت‌شده‌ی دوره یا مدرس |
| روش پرداخت | ✳️ | |
| شماره فیش / شناسه پرداخت | | |
| عکس یا PDF فیش | | چند فایل هم می‌شود |
| ثبت‌کننده | ✳️ | چه کسی دارد ثبت می‌کند |
| توضیح | | |

### ۲) فرم هزینه

```
https://arezoo000.app.n8n.cloud/form/hazine-farda
```

اجباری: **دسته هزینه** (فهرست آماده) · **مبلغ** · **تاریخ پرداخت** · **شرح** · **روش پرداخت** · **ثبت‌کننده**.
اختیاری: طرف حساب · شماره فیش · عکس/PDF فاکتور · توضیح.

⚠️ حقوق مدرسین را اینجا ثبت **نکنید** — خودکار از روی واریزی‌ها حساب می‌شود.

فیلدهای ستاره‌دار را خود فرم اجباری می‌کند: تا پرشان نکنید دکمه‌ی ارسال کار نمی‌کند و هیچ پیامی هم به تلگرام نمی‌رود.

**آپلود فاکتور:** فایل (`.jpg` `.png` `.webp` `.heic` `.pdf`) در گوگل‌درایو، در پوشه‌ی **«فاکتورها و رسیدهای آموزشگاه فردا»** ذخیره می‌شود و لینکش در ستون `لینک_فاکتور` همان ردیف می‌نشیند. اگر آپلود شکست بخورد، رکورد مالی باز هم ثبت می‌شود و فقط ستون لینک خالی می‌ماند.

### ۳) وبهوک POST — برای سامانه‌های دیگر

هر سامانه‌ای — درگاه پرداخت، اپ باشگاه مشتریان، یک اسکریپت ساده، حتی Shortcuts آیفون — می‌تواند به این آدرس `POST` بزند:

```
آدرس وبهوک در پنل خصوصی مدیر است.
```

### تأیید تلگرامی — اختیاری

اگر خانه‌ی `تلگرام_چت_آیدی` در شیت **تنظیمات** پر باشد، از هر سه در که وارد شود خلاصه‌ی رکورد با دو دکمه‌ی **✅ ثبت کن** و **❌ رد کن** به تلگرام شما می‌رسد و تا دکمه را نزنید چیزی در شیت‌های مالی نوشته نمی‌شود.

اگر آن خانه خالی باشد **هیچ داده‌ای از دست نمی‌رود**: رکورد مستقیم ثبت می‌شود و در ستون `منبع` عبارت «(بدون تأیید)» می‌آید — ولی طبیعتاً نه درخواست تأییدی می‌آید و نه پیام «ثبت شد». برای روشن‌کردنش، فرم «تنظیم تلگرام» را یک بار پر کنید (مرحله‌ی ۷ بخش «راه‌اندازی»).

ستون `منبع` نشان می‌دهد رکورد از فرم آمده یا از وبهوک، و ستون `ثبت_کننده` نام کسی را که فرم را پر کرده نگه می‌دارد.

**واریزی فراگیر:**
```json
{ "نوع": "درآمد", "مبلغ": 800000, "پرداخت_کننده": "سارا محمدی",
  "پرداخت_کننده_نوع": "فراگیر", "کلاس": "C001", "تاریخ": "1405/05/18",
  "درصد_مدرس": 70,
  "روش_پرداخت": "کارت به کارت", "شماره_پیگیری": "123456",
  "ثبت_کننده": "فاطمه" }
```

**فاکتور هزینه:**
```json
{ "نوع": "هزینه", "مبلغ": 12000000, "دسته": "اجاره",
  "شرح": "اجاره مرداد", "طرف_حساب": "آقای رضایی",
  "عکس_فاکتور": "https://..." }
```

| نکته | توضیح |
|---|---|
| کلیدها | فارسی یا انگلیسی (`amount`, `payer`, `date`, `category`, `vendor`, `method`, `ref`) |
| اجباری | فقط `مبلغ`. اگر برای واریزی `پرداخت_کننده` نفرستید، «نامشخص» ثبت می‌شود و در پیام تأیید هشدار می‌آید |
| قالب مبلغ | `20.000.000` ، `20,000,000` ، `۲۰٬۰۰۰٬۰۰۰` یا `20000000` — همه درست خوانده می‌شوند |
| `نوع` | اختیاری. اگر نباشد: وجود `دسته` یا `طرف_حساب` یعنی هزینه، وگرنه درآمد |
| `تاریخ` | شمسی، میلادی یا خالی (= امروز). عدد فارسی هم خوانده می‌شود |
| `کلاس` | کد یا نام کلاس؛ مدرس همان کلاس خودکار پیدا می‌شود |
| `نام_مدرس` | فقط وقتی لازم است که دوره در شیت «کلاس‌ها» نباشد |
| `درصد_مدرس` | اختیاری؛ اگر بفرستید بر همه‌چیز اولویت دارد |
| `نوع_درآمد` | اگر نفرستید از روی `پرداخت_کننده_نوع` و بعد کلاس و شرح تعیین می‌شود |
| مهلت تأیید | ۷ روز. بعد از آن رکورد فقط در `لاگ_ورودی` می‌ماند |
| بدون تأیید | کلید `تلگرام_تأیید_لازم` را در «تنظیمات» روی `خیر` بگذارید |
| `ثبت_کننده` | نام واردکننده؛ در فرم یک فیلد جدا دارد، در وبهوک کلید `ثبت_کننده` |
| فایل | فقط از راه فرم. در وبهوک به‌جایش لینک بفرستید (`عکس_فاکتور`) |

> آدرس وبهوک و آدرس فرم هر دو عمومی‌اند. **تا وقتی تأیید تلگرامی روشن باشد**، بدترین حالتِ سوءاستفاده یک پیام مزاحم در تلگرام است، چون هیچ ثبتی بدون فشردن دکمه انجام نمی‌شود. اگر تأیید خاموش باشد، هر کسی که آدرس را داشته باشد می‌تواند ردیف بسازد — پس یا شناسه چت تلگرام را پر کنید، یا در نودهای «وبهوک ورودی»، «فرم واریزی» و «فرم هزینه» گزینه‌ی Authentication را از `none` بردارید.

---

## منطق حسابداری

### درآمد در برابر آورده سرمایه‌گذار
پول سرمایه‌گذار **درآمد نیست** — تزریق سرمایه است. بنابراین:
- در شیت جداگانه‌ی `آورده_سرمایه` ثبت می‌شود، نه `درآمد`
- در **سود عملیاتی** نمی‌آید
- در **جریان نقد خالص** می‌آید (`سود عملیاتی + آورده`)

این تفکیک باعث می‌شود عدد سود واقعی آموزشگاه را ببینید، نه عددی که با تزریق پول رنگ شده باشد.

### سهم مدرس

**مبنای حقوق = پولی که وصول شده.** حقوق هر مدرس در پایان ماه = جمع ستون `سهم_مدرس` روی واریزی‌های همان ماه در شیت «درآمد». نه از روی جلسات، نه از روی شهریهٔ اعلامی — فقط پولی که واقعاً آمده.

اولویت انتخاب درصد، از بالا به پایین (اولین مقداری که پر باشد برنده است):
1. عددی که خودتان در فرم می‌نویسید
2. `درصد_مدرس_اختصاصی` آن دوره در شیت «کلاس‌ها»
3. `درصد_حضوری` یا `درصد_آنلاین` همان مدرس در شیت «مدرسین»
4. درصد پیش‌فرض در شیت «تنظیمات»

درصد در **لحظه‌ی ثبت** قفل می‌شود. یعنی اگر بعداً درصد یک مدرس را عوض کنید، رکوردهای قبلی دست‌نخورده می‌مانند — رفتار درست حسابداری.

**تقسیم کار بین شیت‌ها:**

| شیت | چه چیزی نگه می‌دارد | نقشش در پول |
|---|---|---|
| `درآمد` | `درصد_مدرس` · `سهم_مدرس` · `سهم_آموزشگاه` روی هر واریزی | **مبنای حقوق** |
| `برنامه_کلاس‌ها` | تاریخ، روز هفته، ساعت، کلاس، مدرس، تعداد جلسه و فراگیر، برگزار/لغو | فقط شمارش جلسات در گزارش — **هیچ اثری در پول ندارد** |
| `برنامه_هفتگی` | چه فراگیری، چه کلاسی، چه روزی، چه ساعتی — برنامهٔ ثابت هفته | فقط برنامه‌ریزی — **هیچ اثری در پول ندارد** |

شیت قدیمی `جلسات_تدریس` بازنشسته شد و در هیچ محاسبه‌ای نمی‌آید. اگر مدرسی در شیت «مدرسین» نباشد ولی سهمش روی واریزی ثبت شده باشد، باز هم در فهرست حقوق می‌آید — پول کسی گم نمی‌شود.

اگر «درآمد ناخالص» را خالی بگذارید:
```
(شهریه_هر_نفر ÷ تعداد_جلسات_دوره) × تعداد فراگیر حاضر × تعداد جلسه
```

حقوق نهایی هر مدرس در پایان ماه:
```
خالص = (مجموع سهم جلسات) + (حقوق ثابت در صورت قرارداد ثابت/ترکیبی) − کسورات
کسورات = (درصد_مالیات + درصد_بیمه) × ناخالص
```

### ارزش تورمی سرمایه‌گذاران
برای هر آورده سه ارزش هم‌زمان نگه داشته می‌شود:

| روش | فرمول |
|-----|-------|
| طلایی | گرم طلای ثبت‌شده در روز آورده × نرخ طلای امروز |
| دلاری | معادل دلار در روز آورده × نرخ دلار امروز |
| تورمی | مبلغ اسمی × Π(۱ + نرخ تورم سال‌های بعد از آن) |

هنگام ثبت آورده از طریق فرم، نرخ لحظه‌ای طلای ۱۸ عیار و دلار از **Nerkh API** گرفته و معادل گرم طلا/دلار ذخیره می‌شود. این دقیق‌ترین روش است چون به‌روزرسانی سالانه لازم ندارد.

برای آورده‌های قدیمی که نرخ روزشان را ندارید، خودکار از روش **تورمی** استفاده می‌شود؛ نرخ‌های سالانه در شیت `شاخص_سالانه` است.

> ⚠️ اعداد تورم پیش‌فرضِ شیت `شاخص_سالانه` تقریبی‌اند و برچسب «بازبینی کنید» دارند. قبل از استناد رسمی، آن‌ها را با آمار بانک مرکزی / مرکز آمار ایران جایگزین کنید.

ستون `سود_تورمی_تحقق_نیافته` یعنی «اگر همان پول در تاریخ آورده به طلا تبدیل شده بود امروز چقدر می‌ارزید منهای مبلغ اسمی». این **بدهی ثبت‌شده نیست** — فقط تصویر قدرت خرید است.

---

## تقویم شمسی

همه‌ی دوره‌بندی‌ها شمسی است، نه میلادی:
- ماه‌ها: فروردین تا اسفند (۳۱/۳۰/۲۹ روز، سال کبیسه درست تشخیص داده می‌شود)
- سه‌ماهه: Q1=فروردین‑خرداد، Q2=تیر‑شهریور، Q3=مهر‑آذر، Q4=دی‑اسفند
- شش‌ماهه: H1=فروردین‑شهریور، H2=مهر‑اسفند

پیاده‌سازی تبدیل تاریخ در `lib/jalali.js` است و همان کد فشرده‌شده داخل نودهای Code هر ورک‌فلو اینلاین شده. صحت آن روی **۴۰٬۰۰۰ روز پیاپی (۱۹۹۰ تا ۲۰۹۹)** با رفت‌وبرگشت کامل تست شده است — صفر مغایرت.

---

## نکات عملیاتی

- **اجرای دوباره خطر ندارد:** گزارش ماهانه و حقوق با کلید `شناسه_دوره` به‌روزرسانی می‌شوند (upsert)، نه اضافه‌شدن ردیف تکراری.
- **اگر API نرخ در دسترس نبود:** ثبت انجام می‌شود و در ستون توضیح علامت می‌خورد؛ محاسبه‌ی ارزش با تورم رسمی ادامه می‌یابد.
- **گزارش دوره‌ای از شیت `گزارش_ماهانه` تغذیه می‌شود** — پس WF-04 وقتی درست کار می‌کند که WF-03 ماه‌ها را بسته باشد.
- **همه‌ی ثبت‌ها در `لاگ_ورودی`** با داده خام بایگانی می‌شوند.
- **وقتی تأیید تلگرامی روشن است، ورودی تا تأیید نشود ثبت نمی‌شود** — رد یا بی‌پاسخ‌ماندنِ دو روزه فقط در `لاگ_ورودی` بایگانی می‌شود. وقتی خاموش است، مستقیم ثبت می‌شود و در `منبع` علامت «(بدون تأیید)» می‌خورد.
- **واردات اکسل فقط `.xlsx`** — فایل‌های `.xls` و `.csv` را در اکسل با Save As تبدیل کنید.

---

## ساختار این پوشه

```
school-accounting/
├── README.md                  ← همین فایل
├── docs/
│   ├── guide.artifact.html    ← سورس راهنمای تصویری (لینک بالای همین فایل)
│   └── spreadsheet-schema.md  ← ستون‌های دقیق هر ۱۵ شیت
├── lib/
│   └── jalali.js              ← تبدیل تاریخ شمسی (مرجع + تست‌شده)
└── workflows/
    ├── 00-setup-spreadsheet.ts
    ├── 01-quick-intake-form.ts
    ├── _wf05-normalize.js       ← کد نود «نرمال‌سازی» ورک‌فلوی ۰۵
    └── _wf05-normalize.test.js  ← تست آفلاین همان کد (node _wf05-normalize.test.js)
```

سورس ورک‌فلوهای ۰۲ تا ۰۵ داخل خود n8n نگهداری می‌شود؛ برای گرفتن نسخه‌ی JSON از منوی ورک‌فلو گزینه‌ی **Download** را بزنید.
