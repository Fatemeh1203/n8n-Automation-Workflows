# Lessons (index)

Portable self-improvement log. Curated index — one-line entries under `## <topic>` headings, move long detail into `memory/<topic>.md` and link it from the entry. Every fact lives in exactly one place. See `.claude/rules/self-improvement.md`.
